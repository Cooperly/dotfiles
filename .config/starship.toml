format = """
$username\
$hostname\
[](bg:#a696ba fg:#d8c2f2)\
[ ](bg:#a696ba fg:#1d1a20)\
$directory\
[](fg:#a696ba bg:#80738f)\
$git_branch\
$git_status\
[](fg:#80738f)\
$line_break\
$line_break\
$character\
"""

# Disable the blank line at the start of the prompt
add_newline = false

# You can also replace your username with a neat symbol like   or disable this
# and use the os module below
[username]
show_always = true
style_user = "fg:#1d1a20 bg:#d8c2f2"
style_root = "fg:#1d1a20 bg:#d8c2f2"
format = '[  󰣇 $user]($style)'
disabled = false

[cmd_duration]
min_time = 1
format = 'took [$duration](bold #33658A) '

[character]
success_symbol = "[ ❯](#d8c2f2)"
error_symbol = "[ ❯](#F38BA8)"

[hostname]
ssh_only = false
style = "fg:#1d1a20 bg:#d8c2f2"
format = '[@$hostname  ]($style)'
disabled = false

# An alternative to the username module which displays a symbol that
# represents the current operating system
[os]
style = "fg:#1d1a20 bg:#d8c2f2"
disabled = false # Disabled by default

[directory]
style = "fg:#1d1a20 bg:#a696ba"
format = "[ $path ]($style)"
truncation_length = 9999
truncation_symbol = "…/"

# Here is how you can shorten some long paths by text replacement
# similar to mapped_locations in Oh My Posh:
[directory.substitutions]
"Git" = " "
"Documents" = " "
"Downloads" = " "
"Music" = "󰝚 "
"Pictures" = " "
# Keep in mind that the order matters. For example:
# "Important Documents" = "  "
# will not be replaced, because "Documents" was already substituted before.
# So either put "Important Documents" before "Documents" or use the substituted version:
# "Important  " = "  "

[fill]
symbol = ' '

[git_branch]
symbol = ""
style = "bg:#80738f fg:#1d1a20"
format = '[ $symbol $branch ]($style)'


[git_status]
format = '[$all_status$ahead_behind]($style)'
style = "bg:#80738f fg:#1d1a20"
conflicted = "🏳"
up_to_date = " "
untracked = " "
ahead = "⇡${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"
behind = "⇣${count}"
stashed = ""
modified = " "
staged = '[++\($count\)](bg:#80738f)'
renamed = "襁"
deleted = ""
